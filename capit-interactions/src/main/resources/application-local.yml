spring:
  application:
    name: capit-schedule
  datasource:
    url: jdbc:postgresql://localhost:5432/capit
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: capit-schedule
server:
  port: 8081

axon:
  axonserver:
    servers: localhost:8124



